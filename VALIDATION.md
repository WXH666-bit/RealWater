# 本地验证记录

2026-09-17，Windows 本机，系统报告 NVIDIA GeForce RTX 4060 Laptop GPU，Codex 内置 Chromium 浏览器。清水运动更新后，在 1077×898 CSS 像素窗口中，中档 0.85 渲染倍率观察到 58～59 FPS、模拟时间比 0.995～1.003；高档 1.0 倍率的一次观察为 58 FPS、时间比 0.996。此前页面节流时曾显示约 1 FPS，不能将后台调度当作前台性能。下面的物理场景采用固定步批量加速，其运行耗时不代表真实交互帧率。未进行手机真机性能或传感器验证。

## 自动检查

- `npm test`：26 项通过，包括三档粒子初始化、容量拒绝、12～240 FPS 与交替慢帧下的固定步时钟、不同方位和仰视下的平移/倾斜方向，以及球形水滴、平整液面、邻近融合/拉开分离、表面噪声抑制、整体波峰保留和重建纹理范围。
- `npm run build`：类型检查和生产构建通过。Three.js 分包约 504 kB，仍有 Vite 默认 500 kB 提示。
- `npm audit --audit-level=moderate`：0 个漏洞。

## GPU 场景

| 场景 | 结果 |
| --- | --- |
| 低档静置 60 模拟秒 | 3,600 / 3,600 粒子保留，无流出、无非有限数值 |
| 中档静置 60 模拟秒 | 8,976 / 8,976 粒子保留，无流出、无非有限数值 |
| 高档静置 60 模拟秒 | 18,480 / 18,480 粒子保留，无流出、无非有限数值 |
| 中档急停 | 容器在 0.15 秒内移动 7cm 后停下；水的质心先滞后、再往返，主波周期约 0.9 秒；8,976 粒子全部保留 |
| 中档持续摇晃 120 模拟秒（清水运动更新后） | 2,948 粒子保留，6,028 粒子流出并回收，非有限数值为 0 |
| 倾斜约 74.5° | 水从开口流出，容器内仍有剩余水；非有限数值为 0 |
| 8 秒连续注水 | 8,976 → 11,408 粒子，新增 2,432，未丢失原有水 |
| 实际点击缸口 | 8,976 → 9,014 粒子，单滴加入 38 个粒子 |
| 超容量请求 | 拒绝注水，已有粒子和累计加水数保持不变 |
| 多次重置和切换画质 | 液面鼓包修正增加一张距离平方纹理，完全分配后稳定在 21 个纹理，未出现逐次增加 |

实际浏览器中检查了空白处拖动绕行、容器拖动、方向键摇晃、暂停/恢复、不同视角、玻璃和液面合成；控制台未出现着色器或运行错误。WebMCP 的添加、重置和参数拒绝路径也经过检查。

顶部工具栏更新后，额外通过实际按钮验证模式切换、单滴加入、连续加水、暂停停止注水和禁用加水按钮、重置恢复，以及画质延后应用。连续加水使中档粒子数由 8,976 增至 9,888，暂停后「加水」恢复关闭状态；选择高档并重置后初始化为 18,480 粒子。水滴大小滑杆可通过键盘调整。390px 手机视口下工具栏在顶部排为两行，设置菜单在视口内，无横向溢出。该检查不替代真机触屏和体感验证。

## 连续三维水面更新

旧方案在屏幕上平滑粒子深度，仍会留下圆面轮廓。本次改为世界坐标中的加权粒子隐式表面；静止液面、空中水滴和倾倒后的水流共用该表面，显示法线从三维场梯度计算。已删除旧渲染器。

- 三档「水滴体积」场景在 0.0889 模拟秒暂停：低档 3,600 + 16 = 3,616，中档 8,976 + 38 = 9,014，高档 18,480 + 79 = 18,559；均无流出和非有限数值。可见水滴形成单个有体积的表面，半缸水形成连续液面。
- 检查正常着色及法线显示，并在暂停时旋转视角，确认水滴使用曲面法线、水体表面不随视角变成面向相机的圆片。
- 中档倾倒 8.011 模拟秒：2,461 粒子保留、6,515 已离开并回收，合计 8,976，无非有限数值；容器内连续液面与缸外飞溅可见。控制台无着色器或运行错误。
- 加入仅开发模式启用的 GPU 水面计时。此次抽样受调度/负载影响明显：中档未暂停时观察到约 6–31ms，高档暂停时一次约 54ms；只包含水面管线，不能据此声称桌面 60 FPS 或手机 30 FPS 达标。暂停时复用三维场，计时与运行中也不可直接比较。性能仍须在前台持续交互及真实手机上复测。

## 清水运动更新

- 旧每步 1% PIC 混合会在约 0.77 秒内消去一半网格未解析的速度；改为按时间计算的 8 秒半衰期，并将 80% 的粒子剩余速度用于位置平流。没有提高重力或统一加快播放。
- 相同中档急停场景对比：第 5～6 模拟秒，水体质心在一个秒窗中的峰峰值由 0.00551 增至 0.01887 场景单位，第 6～7 秒由 0.00163 增至 0.01175。两次均保留全部 8,976 粒子、无非有限数值；细小波动保持更久。
- 三档各复测静置 60 模拟秒，分别保持 3,600 / 8,976 / 18,480 粒子，均无流出和非有限数值。低、中、高档结束时速度均方根分别约 0.054 / 0.042 / 0.040 场景单位每秒。
- 固定步进预算从 4 增至 8，保留慢帧的零散时间，长中断丢弃。单元测试覆盖 12、15、20、30、60、120、240 FPS 及交替慢帧，累计模拟时间误差不超过一个固定步。
- 提高稀疏水滴间的表面融合门槛，回归测试验证拉开的水滴不会被假水丝连接；连续水面和单滴球面测试继续通过。缩小重建体积约一半，近处保持原有分辨率，远处水滴沿用球面求交，物理粒子未被删除。
- 本次正常运行抽样中，中档水面 GPU 耗时约 1.6～3.2ms，高档一次约 4.4ms；整页帧率与时间比见开头。该抽样不能替代长时间跨设备基准。

## 液面鼓包修正

本轮针对用户明确指出的「水面鼓包，像一层胶」，保留物理运动参数，调整表面重建和光学表现。

- 重建邻域由 2.5 倍粒子间距扩至 3.5 倍；密集水面与分散水滴分别使用相应半径。额外的距离平方统计阻止宽邻域把分离水滴错误连接。空间滤波和法线采样抑制粒子尺度的小起伏，不使用跨帧平滑。
- 确定性回归样例中，同一组不规则粒子的液面高度噪声均方根小于旧核的 70%；大尺度波浪的原始峰峰值为 1.2 粒子间距，重建后仍大于 0.9。单滴球面、空隙和最低画质采样覆盖检查通过。
- 同一中档「液面形态」在 1.211 模拟秒暂停：前后粒子数 8,976、最高点 0.11608、速度均方根 0.72846 均相同，用于单独对照表面变化。新版不再把每个局部粒子起伏突出成圆鼓包；正常着色和侧面检查中整体波峰仍可见。
- 低档水滴保留 3,616 粒子并能看见空中水滴；高档液面形态保留 18,480 粒子，旋转后仍是连续水体；均无非有限数值和着色器错误。资源数为 21，包含新增的一张距离平方纹理。
- 更新反射光源和屏幕折射方向，减弱灰色厚表皮观感。仍是屏幕空间折射近似，不是多次折射的光线追踪；复杂遮挡和贴近观察时可能有光学误差。之前的物理与性能记录保留为历史证据，不能将暂停画面的帧率视为新版本全场景性能。

## 实现边界

当前为实时 PIC/FLIP 近似：保持粒子质量不等于精确保持几何液体体积，粗网格和有限压力迭代仍有误差。没有工程级精度保证，也不模拟空气、泡沫、微观毛细现象或精确焦散。快速摇晃、低画质及很近的视角下，液面边缘仍可能出现粒子重建痕迹。

移动视口检查仅用于布局；真正的双指手势、体感授权、方向校准以及手机 30 FPS 目标需要在实际手机上继续验证。
